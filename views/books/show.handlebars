<h1>{{book.title}}</h1>
{{#if authors}}
  Written by {{#each authors}}{{this.firstName}} {{this.lastName}}{{#unless @last}}, {{/unless}}{{/each}}
{{/if}}
<p>Originally published in {{book.publishingYear}}</p>
{{#if genres}}
<p>Genre: {{#each genres}}{{this.storyType}} {{#unless @last}}, {{/unless}}{{/each}}</p>
{{/if}}